# ============================================
# DEVELOPMENT MODE (Local development only)
# ============================================
# Set to 'true' ONLY for local development without Supabase
# WARNING: This bypasses ALL authentication!
# NEVER set this in production - it only works with 'vite dev' anyway
# VITE_DEV_MODE=true

# Supabase Configuration (required for production)
# Get these from your Supabase project settings
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ============================================
# GOOGLE DRIVE BACKUP CONFIGURATION
# ============================================

# Google OAuth (for user manual backup via browser)
# Get these from Google Cloud Console > APIs & Services > Credentials
# NOTE: CLIENT_SECRET must NOT have VITE_ prefix (server-side only for security)
VITE_GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_client_secret_here
VITE_GOOGLE_REDIRECT_URI=http://localhost:5173/auth/google/callback

# Google Service Account (for Vercel Cron automatic backup)
# Get these from Google Cloud Console > Service Accounts
# Download JSON key file and extract email and private_key
GOOGLE_SERVICE_ACCOUNT_EMAIL=backup-cron@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here...\n-----END PRIVATE KEY-----\n"

# Backup Encryption (server-side only - NOT exposed to client)
# Generate a random 32-character string for encryption
# You can use: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
# NOTE: Do NOT add VITE_ prefix - encryption happens on server only
BACKUP_ENCRYPTION_KEY=your_32_character_encryption_key_here

# Email Alerts (SendGrid)
# Get API key from SendGrid Dashboard
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
ALERT_EMAIL=your_email@example.com

# Cron Secret (for Vercel Cron authentication)
# Generate a random secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your_random_cron_secret_here
